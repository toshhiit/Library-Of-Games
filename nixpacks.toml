# nixpacks.toml

providers = ['python', 'node']

# 👇 Добавь этот блок, чтобы зафиксировать версию Python
[variables]
PYTHON_VERSION = '3.11'

[phases.build]
cmds = [
    'pip install -r requirements.txt',
    'npm install',
    'npm run build'
]

[start]
cmd = 'python server.py'
